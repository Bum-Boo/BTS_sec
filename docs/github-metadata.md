# GitHub Metadata Note

GitHub About metadata, topics, website/demo URL, pinned order, and social preview are manual GitHub UI settings. Repository files can document the intended values, but they do not update the GitHub UI automatically.

## Copy-Paste Values

- Public display name: `BTS Sec`
- Repository name: `BTS_sec`
- Description: `Defensive security audit toolkit for authorized web and local projects.`
- Topics: `security`, `defensive-security`, `audit`, `web-security`, `static-analysis`, `dependency-scanning`, `secrets-scanning`, `sarif`, `owasp`
- Recommended pinned position: `6`
- Website/demo URL: `TODO: add sample report or case study URL`
- Social preview: `TODO: add safe report summary screenshot`

## Naming Note

Use `BTS Sec` as the public-facing name. `VibeSec` is an internal/alternate naming direction and should not be used as the main public name yet. The `vibesec` CLI alias may remain for compatibility while public docs lead with BTS Sec.

## Safety Note

Keep the defensive framing visible: authorized use only, passive/static checks by default, no exploit execution, no brute force, no credential theft, no destructive payloads, redacted evidence, and manual review required.
